services:
  web:
    ports:
      - '127.0.0.1:${WEB_PORT:-3000}:3000'

  api:
    ports:
      - '127.0.0.1:${API_PORT:-3001}:3001'

  reporting-api:
    ports:
      - '127.0.0.1:${REPORTING_API_PORT:-3002}:3002'

  database:
    ports:
      - '127.0.0.1:${POSTGRES_PORT:-5432}:5432'

networks:
  data:
    internal: false
